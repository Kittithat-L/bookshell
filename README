Install https://nodejs.org/en
first go to backend and type "node index.js" to start backend server
next go to frontend and type "npm start" to start frontend server
backend ip = "localhost:5000"
frontend ip = "localhost:3000"

How To Check Install Node.js
open cmd and type "node --version"
